//console.log("projeto 1")
//alert("Ola mundo");

//variável

let nome = 'Ítalo';
let nome1 = 'Luisa';
let filho = 'Aurora';

console.log(nome, ' nasceu em 1984.', );
console.log('Em 2000 ', nome, ' conheceu ', nome1);
console.log(nome, ' casou-se com ', nome1, ' em 2012');
console.log(nome1, ' teve 1 filho com ', nome, ' em 2015');
console.log('O filho de ',nome, ' se chama ', filho);

// Não podemos criar variáveis com palavras reservadas

/*Os seguintes são reservados apenas quando o código está em modo estrito (strict mode):

implements
interface
let
package
private
protected
public
static
Os seguintes são reservados apenas quando são encontrados no código do módulo:

await
Palavras-chave reservadas para uso futuro em versões antigas
As seguintes palavras-chave foram reservadas para uso futuro em especificações antigas do ECMAScript (ECMAScript 1 até 3).

abstract
boolean
byte
char
double
final
float
goto
int
long
native
short
synchronized
throws
transient
volatile
Além disso, os literais null, true, e false não podem ser usados como identificadores em ECMAScript. */

// Sempre seguir nomes de variáveis conforme a atribuição da mesma

//Não pode começar nome da variável com número
//let 1nome

// Não pode conter espaço ou traços 
//Utilizamos CamelCase 

//let nome completo - errado
//let nomeCompleto - correto 

//Case- sensitive 
// Letra maiuscula e letra minuscula são diferentes

// Não utilize var, utilize let