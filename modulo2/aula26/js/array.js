// Array é uma lista/ coleção de coisas
//Array é do tipo object


const nome = ['Maria', 'João', 'Carla', 'Brenda', 'Barbara', 'Hafsah'];

console.log(nome instanceof Array); // Para saber se o elementos que estamos trabalhando é uma instancia de array;




//const removido = nome.pop(); //Remove elemento do final do array
//const removido1 = nome.shift();  //Remove elemento do começo do array

//delete nome[3] // Elemina o valor do indice informado, porém, não altera os valores do indice 

//nome.slice(0,3) // Fatia o array conforme o indice que foi informado.


//console.log(removido, removido1)
//nome[nome.length] = 'Iva'   // Coloca o valor informado na última posição da nossa lista //Adiciona no fim
//nome.unshift('Karol') // Adiciona o valor informa na primeira posição da lista [0] e move os ouutros para frente. 
//nome[nome.length] = 'Italo' // Coloca o valor informado na última posição da nossa lista //Adiciona no fim
//nome.push('Otavio') // Função para colocar o nome na última posição da nossa lista // Maneira mais indica de colocar // //Adiciona no fim
//nome[nome.length] = 'Luiz' // Coloca o valor informado na última posição da nossa lista //Adiciona no fim
//nome[nome.length] = 'Marta' // Coloca o valor informado na última posição da nossa lista //Adiciona no fim

//console.log(nome.length) // Saber quantos indices tem o array

//nome[0] = 'Eduardo';
//nome[6] = 'Luisa'; // Adicionar um elemento assim na última posição é arriscado já que precisaria saber exatamente o tamanho do array e esse tamanho sempre pode mudar

//for (var i= 0; i<nome.length; i++){
 //   console.log(nome[i])
//}
